import styled from 'styled-components'

export const ContentTextWrapper = styled.section`
    box-sizing: border-box;
    width: 100%;
    height: auto;
    display: flex;
    flex-flow: column nowrap;
`